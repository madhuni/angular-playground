import { TextToolTipDirective } from './text-tool-tip.directive';

describe('TextToolTipDirective', () => {
  it('should create an instance', () => {
    const directive = new TextToolTipDirective();
    expect(directive).toBeTruthy();
  });
});
