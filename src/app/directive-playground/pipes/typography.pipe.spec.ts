import { TypographyPipe } from './typography.pipe';

describe('TypographyPipe', () => {
  it('create an instance', () => {
    const pipe = new TypographyPipe();
    expect(pipe).toBeTruthy();
  });
});
